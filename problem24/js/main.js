var downstream=prompt(" enter downstream");
downstream=parseInt(downstream);
var upstream=prompt(" enter upstream");
upstream=parseInt(upstream);
var speed;
var speed=1/2*(downstream-upstream);
alert(speed);